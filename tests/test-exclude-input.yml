name: Test exclude input - non-PR context
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Test that the action accepts the 'exclude' input (newline-separated glob
      # patterns) without crashing on input parsing. The action will still fail
      # because there is no pull_request context under workflow_dispatch, but
      # the failure should be due to the missing PR — not a bad input error.
      - id: action
        uses: .
        with:
          github-token: ${{ github.token }}
          exclude: |
            docs/**
            *.md
            vendor/**
        continue-on-error: true

      - name: Assert action failed due to missing PR context, not bad input
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail in non-PR context, got outcome=$OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed when run outside a pull_request context (exclude input accepted)"
